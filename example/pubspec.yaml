name: eps_payment_gateway_example
description: Demonstrates eps_payment_gateway with both full-screen and modal payment flows.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  eps_payment_gateway:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
